package tracer
// internal/app/tracer/tracer.go
func InitTracer(serviceName string) func() {
    // Connects to Jaeger running in Docker
    // Returns a closer function to run on shutdown
	return nil
}
